{
  "name": "analytics-service",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",  // Added main entry point
"scripts": {
  "start": "ts-node --project tsconfig.json src/index.ts"
  "dev": "ts-node --project tsconfig.json --transpile-only src/index.ts"  // Added dev command for faster startup
  
},
  "dependencies": {
    "@repo/db": "workspace:*"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
